{
  "name": "fire-pwa-root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd fire-pwa-blue && npm run dev --",
    "build": "cd fire-pwa-blue && npm run build && cd .. && rm -rf public && cp -R fire-pwa-blue/dist public"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "firebase": "^10.8.0",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "serve": "^14.2.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "protobufjs"]
  }
}
